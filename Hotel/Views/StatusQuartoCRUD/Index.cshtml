@model IEnumerable<Hotel.Models.poco.StatusQuarto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StDescridao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StFlAlugavel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StDescridao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StFlAlugavel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StId })
        </td>
    </tr>
}

</table>
