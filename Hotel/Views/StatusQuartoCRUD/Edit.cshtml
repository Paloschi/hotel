@model Hotel.Models.poco.StatusQuarto

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>StatusQuarto</legend>

        @Html.HiddenFor(model => model.StId)

        <div class="editor-label">
            @Html.LabelFor(model => model.StDescridao)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StDescridao)
            @Html.ValidationMessageFor(model => model.StDescridao)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.StFlAlugavel)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StFlAlugavel)
            @Html.ValidationMessageFor(model => model.StFlAlugavel)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
