@model Hotel.Models.poco.Checkin

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Checkin</legend>

        @Html.HiddenFor(model => model.ChId)

        <div class="editor-label">
            @Html.LabelFor(model => model.ChData)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ChData)
            @Html.ValidationMessageFor(model => model.ChData)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ChPrevisao)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ChPrevisao)
            @Html.ValidationMessageFor(model => model.ChPrevisao)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ChSaida)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ChSaida)
            @Html.ValidationMessageFor(model => model.ChSaida)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ChValor)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ChValor)
            @Html.ValidationMessageFor(model => model.ChValor)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
